[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# Frontend calls fetch("/api/generate") unchanged — this redirect sends
# that request to the Netlify Function under the hood.
[[redirects]]
  from = "/api/generate"
  to = "/.netlify/functions/generate"
  status = 200
